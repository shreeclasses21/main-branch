document.addEventListener('DOMContentLoaded', () => {
    // This script can be used for any future JavaScript functionalities
    // specific to the reset password page, such as:
    // - Client-side validation for the email field
    // - Handling the OTP verification step (if added later)
    // - Displaying success/error messages dynamically

    console.log("Reset password page loaded.");

    // Example: You could add a simple form submission handler here
    // const forgotPasswordForm = document.querySelector('form');
    // forgotPasswordForm.addEventListener('submit', (event) => {
    //     event.preventDefault(); // Prevent default form submission
    //     const email = document.getElementById('email').value;
    //     console.log('Sending OTP to:', email);
    //     // Here you would typically send an AJAX request to your backend
    //     // and handle the response (e.g., show a success message or error)
    // });
});
